<div class="container">
  <h1>My Tasks</h1>
  <ul>
    <% @tasks.each do |task| %>
      <li> <%= task.completed ? '✔' : '[ ]' %> <%= link_to task.title, task_path(task.id) %><%= link_to "🗑", task_path(task.id), method: :delete %></li>
    <% end %>
  </ul>
  <%= link_to "+ Add a new task", tasks_new_path %>
</div>

